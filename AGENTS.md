# 项目顶层规则

## 项目维护默认使用中文

本项目的维护沟通、规则沉淀和说明材料默认使用中文，方便后续查看、理解和继续维护。

适用范围包括：

- 与用户沟通时的进度说明、结果说明、问题分析和操作指导。
- `AGENTS.md` 等项目规则，以及新增或更新的维护文档。
- 面向维护者的脚本提示、配置说明、故障排查说明和发布备注。
- 新增代码注释时，在不违反现有代码规范和外部工具要求的前提下使用中文。

以下内容不强制翻译：

- 可执行的终端命令、代码、参数、环境变量、文件名、接口字段和程序标识符，必须保持正确语法。
- 第三方平台、协议、依赖或发布系统强制要求使用英文的字段。
- 引用外部报错、日志或官方原文时，可保留原文，但解释和处理建议应使用中文。

产品界面存在中文和英文语言切换时，新增或修改用户可见文案必须同步提供中英文两套，并保持含义、功能和更新进度一致。不得只更新其中一种语言，导致两个界面的信息或操作能力不一致。

## GitHub 发布与画布版本同步

当用户要求“上传 GitHub、合并主分支”或同义发布操作时，版本同步属于发布流程的必做项，不能等用户再次提醒。

执行要求：

- 根目录 `VERSION` 是产品版本号的唯一运行时来源；智能画布所在主页面左下角必须通过 `/api/app-info` 读取并显示该版本，不得另设前端硬编码版本号。
- 发布前同步更新 `VERSION`、`README.md` 当前版本和 `static/update-notes.json`，并确保更新说明中的版本与 `VERSION` 完全一致。
- 提交和推送前必须运行版本一致性测试，并通过本机 HTTP 或真实页面确认：`/api/app-info` 返回值、左下角版本显示和 `VERSION` 一致。
- 静态资源 URL 中用于缓存刷新的 `?v=` 标识不等同于产品版本；不得把源文件里的旧缓存标识误判为左下角版本。服务端仍应在响应时按当前 `VERSION` 和资源修改时间动态生成缓存标识。
- 合并主分支并推送后，再检查远端主分支的 `VERSION` 与本地左下角显示。只更新 GitHub、更新说明或后端中的一部分，均不得视为发布完成。

禁止在 `main.py`、前端页面或脚本中维护第二份需要人工同步的产品版本常量。新增版本展示入口时必须复用 `current_app_version()` 或 `/api/app-info`。

## Mac 与 Windows 双平台支持

修改本项目时，默认同时支持 macOS 和 Windows。

以下改动尤其需要执行双平台检查：

- 启动脚本、安装脚本或依赖安装方式。
- Python 运行环境及版本假设。
- 文件路径、路径分隔符、文件名、编码或权限。
- 子进程调用、终端命令、环境变量或外部程序。
- 本地文件、上传、下载、缓存目录、日志或生成资源。
- 浏览器启动行为或本地服务地址。

正确做法：

- 检查 macOS 入口，例如 `mac-启动服务.command`、`mac-启动服务.sh` 和 `mac-安装依赖.sh`。
- 检查 Windows 入口，例如 `run.bat`、`安装依赖.bat` 及相关 `.bat` 脚本。
- 优先使用 `pathlib`、`os`、`subprocess` 参数列表和运行时平台判断等跨平台 Python API，避免把功能绑定到单一终端环境。
- 运行时新增依赖时，同步维护公共依赖清单和各平台安装脚本。
- 某项功能无法在两个系统上保持完全一致时，在对应脚本或面向用户的说明中写明平台差异。

禁止做法：

- 同一工作流程应支持 Windows 时，不得只按当前 Mac 环境实现。
- 不得假设 Windows 支持 POSIX 路径、`chmod`、`xattr`、`open`、Shell 通配符或 Unix 专用命令。
- 不得假设 macOS 支持 `start`、`.bat` 行为、仅反斜杠路径或 `cmd.exe` 语法。

完成任何影响运行行为的更新前，至少在本机真实验证一条 macOS 使用路径，并检查相关代码和脚本的 Windows 兼容性。如果本地无法实际运行 Windows，交付时必须明确说明，并列出已检查的 Windows 文件或命令。

## 底层稳定性与固定回归

- 修复会影响运行、数据、网络提交或跨平台行为的 bug 时，先建立可复现案例，并把案例保留在 `tests/`；新版功能通过不代表旧功能可以跳过回归。
- 固定回归必须能在全新检出、没有个人 API 配置或密钥的环境中执行；使用公开档案和明确测试启用清单，模拟网络使用假 Key。不得以维护者本机配置代替测试输入，也不得为测试通过而自动补回用户删除的模型。
- 本机统一执行 `tools/check_regression.py`。正式 R2 发布必须等待 Mac/Windows × Python 3.10/3.14 回归矩阵成功；不能因本机通过而宣称 Windows 已验证。浏览器手势、系统剪贴板和外部 CLI 仍需对应平台真实检查。
- 安装、启动和受管理服务的更新重启共用 `local_runtime.py`。不得重新引入按端口强杀进程、固定延时打开未就绪页面、安装和启动使用不同解释器、失败后仍显示完成等行为。
- 网络读取可以有界重试；无平台幂等证据的任务提交遇到超时或网关错误不得自动重发。记录请求与任务标识，优先查询原任务，不能将超时解释成上游未接单。
- 索引损坏或格式版本不受支持时必须保留原文件并阻止写回，不能以空库兜底后覆盖。现有同进程文件锁不能作为多进程/多用户并发支持的证据。
- 新页面和能力按职责接入既有存储、模型预检、任务与共享控件；不复制第二套生成链路，不以机械拆文件代替模块边界。每次抽离模块都核对更新包是否包含新增运行文件。

固定场景、当前实现基线和未覆盖边界见 `docs/基础架构与回归验收.md`；影院后续需求见 `docs/影院功能需求.md`。

## laohu API 入口与迁移

laohu 对外品牌、请求域名和公开模型 ID 已更新，但项目内部 provider ID 仍保持 `ai-money`，避免旧画布、旧配置和历史生成记录失效。

- 所有 laohu 实际 API 请求统一使用 `https://api.lao-hu.com`；旧域名只允许出现在迁移识别和回归测试中，不得再作为提交地址。
- 用户界面统一显示品牌名 `laohu`，人类访问入口使用 `https://api.lao-hu.com/`，价格入口使用 `https://api.lao-hu.com/pricing`。
- 2026-09-12 已确认 `/docs/llms.txt` 恢复可用。模型目录以带鉴权的 `https://api.lao-hu.com/v1/models` 为准；参数与请求协议以当前官方文档和适配器证据共同确认，不能把旧文档不可用状态永久固化。
- 历史 `laohuaimoney-*`、`laohuaimoney/*`、`zhenzhen-*` 和 `zhenzhen/*` 模型 ID 在读取时必须单向迁移为 `laohu-*` 或 `laohu/*`；真实提交不得再发送旧 ID。
- 新 Key 统一保存为 `API_PROVIDER_LAOHU_KEY`；旧 `API_PROVIDER_AI_MONEY_KEY` 只作为读取兼容，不得因品牌迁移导致用户已保存的 Key 失效。

## GPT CLI 能力边界

GPT CLI 对应 OpenAI Codex CLI，在本项目中只接入文本能力，不接入图片、视频、音频或音乐生成能力。

正确做法：

- GPT CLI 只出现在聊天模型或现有智能画布提示词节点的文本模型选择中。
- Codex CLI 能够读取图片时，只把图片作为文本任务的参考上下文，最终输出仍然是文本。
- API 设置保存或读取旧 GPT CLI 配置时，主动清空残留的图片模型、视频模型和音频模型。
- 后端必须拒绝把 GPT CLI 当作图片、视频、音频或音乐生成平台调用，不能只依赖前端隐藏入口。
- macOS、Linux 和 Windows 的 GPT CLI 安装脚本只安装 Codex CLI 文本运行所需内容。

禁止做法：

- 不得因为 Codex CLI 支持图片输入，就把它登记成图片生成能力。
- 不得安装或调用 `gpt-image-2-skill` 等图片辅助工具来扩展 GPT CLI 的项目能力。
- 不得把 GPT CLI 显示在图片生成、视频生成、音频生成、音乐生成或 AI 应用执行节点的平台列表中。

## 推荐 API 曝光白名单

API 设置中的“推荐 API”属于管理员背书的公开入口，不等同于项目已适配平台、用户历史配置或可自定义接入的平台全集。

- 只有管理员明确审核并认可的平台才能出现在推荐 API 页面；当前推荐白名单仅包含 `Agnes AI`。
- 新增平台适配、保留历史平台配置或允许用户自定义接入，不得自动把该平台加入推荐 API。
- 从推荐 API 移除平台时，只移除公开推荐入口，不得连带删除平台列表中已经配置好的平台、CLI、模型能力档案或请求适配器。
- 推荐页可以表达“欢迎优质 API 平台入驻”的合作意向，但未经管理员明确确认，不得展示具体第三方平台名称、注册链接、优惠信息或推荐卡片。

## 界面需求直接实现

当用户已经用截图、现有页面和文字明确说明界面目标时，默认直接修改真实页面并完成浏览器验证，不再额外要求用户审批草图、线框图或视觉对比稿。

- 先从现有代码、参考截图和用户描述中提取确定项，直接实现功能与排版。
- 颜色、间距或局部样式存在多种合理选择时，按当前项目风格做保守实现，并让用户基于真实页面继续反馈。
- 只有实现会明显改变功能范围、数据结构、成本或不可逆行为时，才暂停并询问用户取舍。
- 除非用户明确要求看草图，否则不得把视觉草图、方案投票或浏览器对比稿作为开始编码的前置条件。

## 正式平台适配范围

用户要求“全部平台”“所有正式平台”或同义范围时，必须先从当前 API 设置和平台注册表列出完整平台清单，再逐个平台建立全量目录、能力档案、请求适配和无付费验证。当前正式范围包括 RunningHub、laohu、ModelScope、即梦 CLI、GPT CLI 和火山引擎；只有用户明确排除的平台才可以不纳入本次实现与报告。

禁止只完成资料最完整或当前正在讨论的少数平台后，把局部覆盖当作全平台完成。最终报告必须分别给出每个平台的目录总数、可运行数、待补 Schema 数和适配器缺口；GPT CLI 等能力边界较窄的平台也必须回归其禁止能力，不能因为模型数量少而省略。

## 数据分层与迁移规则

新增或修改本地数据功能时，先按数据职责选择目录，不得只因为旧代码已有某个路径就继续混放：

- `data/` 只保存设置、索引、画布 JSON、会话等结构化数据，不保存可直接查看的图片、视频、音频成品。
- `assets/input/` 保存用户输入素材，其中 `asset/` 是长期资产素材，`temporary/` 是临时素材。
- `assets/output/` 保存独立生成结果，按图片、视频、音频、文本和其他文件分类；删除画布不得连带删除生成结果。
- `workflows/` 保存 ComfyUI 工作流和画布工作流，不能伪装成素材文件放进 `assets/`。
- `cache/` 只保存可重新生成、可以安全删除的缓存；`backups/` 只保存备份包；`API/` 保存密钥。
- `data/model_capabilities/` 只保存模型能力档案、统一参数语义、平台字段映射和档案索引，不保存 API Key、用户素材或用户当前启用状态。
- `docs/model-capabilities/` 保存能力档案维护规范、平台适配说明和维护入口，面向维护者阅读，不作为运行时配置来源。

模型能力档案与用户启用列表必须分层维护：官方平台的全量能力档案决定项目理论上能够适配哪些模型；`data/api_providers.json` 中各平台的 `chat_models`、`image_models`、`video_models`、`audio_models` 只决定当前用户在画布里看见并可调用哪些模型。首次添加平台可以播种一套默认模型，但用户后续删除模型后，打开设置、渲染界面、保存配置、刷新或重启服务都不得自动补回；重新启用必须由用户主动添加或重新拉取。

模型能力文件按生命周期分层维护：能力词典和档案格式属于稳定文件；`providers/` 中的平台与模型档案属于高频维护文件；`registry.json` 是程序入口索引；`data/api_providers.json` 仍是用户运行配置。新增或修改模型时，先更新对应能力档案和证据，再让 API 设置引用；不得把模型参数长期硬编码回节点前端。

平台全量适配与用户启用清单必须分层维护。平台拉取到的完整模型目录用于能力覆盖审计，平台中的每个模型都应具备可追溯的能力档案和适配状态；`data/api_providers.json` 中的 `chat_models`、`image_models`、`video_models`、`audio_models` 只表示当前用户主动启用的子集，是画布模型可见和允许调用的唯一白名单。用户删除或不添加某个模型，只能让它退出当前用户的画布候选，不得删除、禁用或掩盖该模型的共享能力档案和平台适配器。能力档案中的历史 `enabled_in_api_settings` 字段不得覆盖用户配置。

文件写入必须遵守以下行为：

- 相同内容使用 SHA-256 去重并复用稳定 ID，重复上传不得反复复制文件。
- 用户可见名称尽量与磁盘文件名同步；同名但内容不同时才追加短哈希避免覆盖。
- 用户界面不得用“图片节点”“视频节点”“音频节点”“文本节点”等标题替代真实素材名称。用户上传的素材优先保留用户原始文件名；媒体类型通过图片预览、视频播放器、音频波形/播放条或文本内容自然呈现。只有在原文件没有名称时，才使用类型化兜底名称。
- 临时素材晋升为资产素材时移动文件并保留稳定引用，不复制第二份。
- 资产和结果通过稳定接口 URL 读取，物理目录移动或文件改名不得让画布引用失效。
- 改变目录结构时必须提供可重复执行的迁移、迁移前备份和旧引用重写；未经验证不得直接删除旧目录。

## 现有项目重构规则

### 融合创作节点与影院画布（2026-09-12 最新需求）

以下规则取代下文早期“执行节点另建结果占位节点”“生成信息弹窗重新运行”的产品行为，旧规则仅用于辨认迁移来源。

- 影院直接在画布组织，不新增菜单、独立页面或内置 Agent / Skill。外部 Agent 调用同级 AI 视觉后填写正文、创作说明、参数与连线。
- 创作节点内显示结果与历史，选中后下方复用共享输入/参数面板。每次运行有独立任务、快照、取消和失败记录，任务不作为新可见节点；并发完成不覆盖其他结果。
- 节点 ID 管位置，creationId 管共用创作，素材 ID 管文件。复制保留关联和引用，不复制文件；未生成共用资产首次产出同步。副本修改配方或单独运行时分支，不改其他副本；原节点持续抽卡时，共用副本持续跟随。creationOwnerNodeId 明确记录这个方向。
- SHA-256 文件去重不代表创作配方相同。导入生成素材恢复有记录的配方，缺记录不得猜模型。
- 完整剧本与分段用文本素材，编辑器右侧只提供自由 Markdown 创作说明，完整保留和展示，不拆成固定字段或必填表单。说明用于人阅读及 Agent 按节点恢复上下文；可以建议记录结论、资产状态、衔接或来源，但不能强迫逐项填空。真实节点 ID、连线和素材引用独立保存，不能靠解析说明文本代替。旧结构化说明单向转成 Markdown，保留所有已有内容。Agent 写回检查修订号，避免覆盖用户刚改的说明。
- 完整剧本说明呈现背景、梗概、人物和创作意图，不要求分段讲戏。分段正文只从已定原文切分，不能由 Agent 改写或摘要替代；分段说明记录实际执行所需的讲戏、资产状态和接续，可按需取舍，仍不做固定填空。
- 创作进度表按已存在的分段节点登记，引用实际剧本、图片、音频/音色、视频节点 ID。表格从节点即时推导状态和预览，不复制媒体或维护第二套完成标记；没有节点不造占位。音色文字设计与实际音频结果必须区分。点击表格项复用画布平滑聚焦，不能瞬移或让节点被表格遮住。
- 界面一致性只约束布局、控件样式与操作方式，不统一不同平台、模型或运行模式的参数内容。选项、范围和真实提交值按对应 API 的已确认契约分别配置；服务端明确列出的合法值不得因旧 CLI 帮助遗漏而直接删除或取交集。发现客户端与服务端资料冲突时核对提交链路，保留证据并明确验证边界，禁止把某个模型的修正扩散到全部模型。
- 分段到资产、音频和视频的所属关系统一使用 `kind:story` 虚线；手动 Shift＋右键拖动与 Agent `relation:story` 复用同一数据源。虚线不得参与模型输入、引用条、参数、运行依赖或循环；删除虚线不得删除同端点的实线。进度表按虚线派生，旧登记数组单向转为虚线。新增快捷键同步更新右上角说明、中英文和 Agent 指南。
- 节点标题和操作浮在媒体上方外侧，不能用带标题栏的大卡片包裹媒体。只有选中节点显示展开工具栏、参数面板及右侧历史版本切换栏，未选中及仅悬停均不显示；版本栏当前项必须清楚高亮，数字间距、背景和悬停反馈保持一致；媒体随画布缩放，工具和参数面板按屏幕尺寸维持可读、易点击；缩放、聚焦和拖拽改尺寸均同步锚点，不替换编辑 DOM。创作进度默认固定展开；固定控制置于标题行内部中央，无边框、底座或外凸。圆圈表示固定，双横杠表示自动收起，点击只原位切换图形。解锁时鼠标仍在面板上不得立即收起；移出后短暂宽限再收起，悬停底部重新展开。
- 新版素材及执行节点统一采用标题操作栏、结果预览、下方输入参数的层次。删除节点只在标题栏右侧保留一个入口；选中节点的适用处理工具在内容上方直接平铺为图标加名称，不用省略号或“更多”菜单隐藏；窄屏允许工具栏横向滚动，不叠在正文或媒体上。历史版本在节点右侧按可用高度从上往下排列，满列后向右新增列；缩放、拖拽改尺寸和平滑聚焦均须检查版本栏边界。
- 节点标题双击原位编辑，Enter 保存、Escape 取消，切换历史版本和生成新结果不能抹掉用户或 Agent 的有意义命名。运行态使用真实任务状态、计时与取消，预览上用毛玻璃和移动光效提示处理中；无可信进度时不显示伪造百分比，并遵守减少动态效果设置。
- 旧执行与结果合并前保存快照，保留历史版本及下游历史引用；重复迁移不产生重复节点或版本。

### 未发布阶段的内核重构原则（2026-09-12）

产品尚未正式发布。用户授权以当前功能、用户数据和生成结果为验收基线，优先提升稳定性、安全、响应和维护效率；旧架构、旧更新器目录和内部接口本身不构成必须兼容的约束。

- 可以替换、拆分和重组内部实现；旧实现没有独立价值时直接退出，不能为保留旧结构增加长期双轨代码。
- 现有用户素材、作品、配置和结果仍需保护。改变数据格式时提供一次性迁移和备份，不能把“不保留旧架构”解释为可以清空用户数据。
- 按职责和依赖拆模块，并通过明确参数或接口协作；禁止把 `main`、全局变量字典或动态 `exec` 作为模块间依赖容器，制造表面拆分。
- 故障隔离需要有可验证行为：慢连接不能拖住其他连接，失败提交不能自动重复付费，局部索引异常不能覆盖其他数据。仅减少文件行数不视为完成。
- 同步更新测试和交付清单。验收保持外部功能与结果一致，内部旧函数名和文件位置不必成为永久约束。

本项目的产品改造默认属于“在现有系统上重构”，不是脱离代码和数据从零设计。编写 PRD、提出交互方案或修改架构前，必须先核对当前项目已经存在的能力、数据结构、接口、异常处理和用户操作路径。

新版智能画布架构是当前产品行为的唯一主线。旧画布能力只有在不改变新版交互、参数语义、数据模型和执行结果，并且对新版仍有明确帮助时，才通过适配层继续复用。旧字段、旧默认值、旧分支或旧输入输出规则一旦与新版设计冲突，必须通过一次性迁移覆盖并退出新版运行链路；不得为了兼容旧画布继续双写、双读或在新参数缺失时静默回退到冲突旧值。历史数据可以保留在迁移前备份和旧文件导入适配层，但不能反向决定新版节点的运行行为。

判断顺序：

1. 先以当前新版节点、当前能力档案和用户最新确认的交互为准。
2. 再检查旧能力是否仍有独立价值且与新版无冲突；满足时做单向适配。
3. 发现同一语义存在新旧两套字段或两套执行分支时，迁移到新版字段后删除旧运行入口。
4. 验收必须确认真实请求、结果记录和重新打开画布都只读取新版数据源，不能只检查界面显示。

触发条件：

- 拆分、合并或新增画布节点。
- 修改素材库、API 设置、模型映射、循环、结果保存等已有功能。
- 用户提供其他产品截图、外部方案或新的交互设想。

执行顺序：

1. 读取相关前端、后端、数据文件和现有文档，建立当前实现基线。
2. 区分已经可用的能力、存在问题但可复用的能力、确实缺失的能力和应淘汰的历史行为。
3. 至少比较“保守迁移”“迁移层重构”“完全替换”三类方案；根据改动范围、数据兼容、用户学习成本和维护成本给出推荐。
4. PRD 中每个重要设计必须写明：现状依据、保留内容、变化内容、迁移方式、兼容风险和验收方法。
5. 先建立新旧能力迁移清单，再进入代码实现；未核对的旧能力不得默认删除。当前智能画布重构形成一个素材节点和六类运行节点：文本生成、图片生成、视频生成、音频生成、音乐生成和 AI 应用。素材节点支持纯素材上传与生成结果展示两种来源状态，并承载文本、图片、视频、音频四种素材。新建文本生成使用 `smart-text-generator`；旧 `smart-prompt` 只作为历史画布兼容入口。迁移完成后旧混合节点只保留在备份和旧数据导入适配层，不保留在正常画布运行体系中。

正确做法：

- 将现有智能画布已经验证有效的预览、连线、循环、任务恢复、动态参数等能力迁移到职责更清晰的新节点。
- 同类交互优先复用项目中已经存在且未被否定的组件、命名和操作路径。新增菜单、命名弹窗、收藏分组或媒体预览前，先检查当前画布和素材库是否已有同类实现；同一操作不得在不同入口使用互相冲突的交互。
- 当前产品页面中的业务提示、危险确认和文本输入不得调用浏览器原生 `alert`、`confirm` 或 `prompt`。统一使用 `StudioDialog.alert`、`StudioDialog.confirm` 和 `StudioDialog.prompt`；确认与输入必须显式 `await` 后再继续业务操作，危险删除或清除使用 `danger` 类型，可能失败或缺少输入使用 `warning` 类型。新增页面必须加载 `static/js/theme.js` 或提供等价的共享弹窗组件，不得重新引入带浏览器地址标题的系统弹窗。
- 按钮触发的菜单或下拉层必须锚定触发按钮，默认紧贴按钮展开，并确保层级高于其服务的节点、素材和容器。除执行节点参数弹层明确采用点击锁定外，展开方向必须有同方向的悬停安全通道，并提供短暂离开宽限，保证鼠标快速或斜向移入选项时不闪退；完全离开后仍须自动关闭。验收时必须检查实际命中栈和快速移动手势，不能只检查菜单看得见。
- 智能画布的节点创建入口统一复用同一套分类纵向菜单。双击空白画布或把连接线松到空白处，都打开这套菜单；后者选择节点后自动完成原节点到新节点的连接，取消菜单不得创建空素材。分组属于框选后的组织操作，只通过快捷键或明确的分组命令创建，不得重新放回节点创建菜单。
- 智能画布中的所有节点统一支持 `Shift + 双击` 聚焦。该动作必须按节点及其当前可见弹层的联合边界计算缩放，平滑移动到视觉中心，并保留参数展开、滚动、播放和运行状态；不得触发该节点原有的普通双击预览、编辑或菜单行为。新增或修改此快捷键时必须同步更新右上角快捷键说明和中英文文案。
- 按住 `Shift` 从节点上起拖时，起线优先于节点内部按钮、输入框、下拉框和文本区域的默认交互；节点无需预先选中。落线命中以“所属节点”为边界，目标节点内部任何控件区域都视为该节点，不得按节点左右半区猜测端口。拖线的移动和结束监听必须在捕获阶段兜底，避免内部控件阻止冒泡后留下临时线。
- 视频播放后默认隐藏中央控制，避免遮挡画面；鼠标进入视频区域时显示暂停控制，移出后再次隐藏。暂停、播放结束或尚未播放时恢复播放控制。播放控件状态必须与实际媒体状态同步，画布重渲染后不得重复绑定事件或出现一次点击连续播放和暂停。
- 外部产品截图只作为功能和交互参考，最终方案必须适配本项目已有架构、视觉风格和数据约束。
- 新设计与当前实现冲突时，明确说明冲突和取舍，不得把猜测写成既定事实。
- `smart-image` 迁移成功后，画布 JSON 使用素材节点、图片生成、视频生成、音频生成、音乐生成和 AI 应用节点；素材节点通过来源状态区分上传素材与生成结果，原混合节点数据通过迁移前备份保留，不能继续作为用户可见节点或运行节点存在。
- `smart-prompt`、`smart-loop`、`smart-group`、`smart-minimax` 以及其他历史专用节点不属于新节点重构范围。不得修改其节点结构、名称、参数、交互、执行逻辑或数据字段，只允许增加它们与新节点连接所必需的兼容适配；新建文本生成不得回退为 `smart-prompt`。
- `smart-text-generator` 可接收文本、图片、视频和音频输入，但只显示能力档案确认支持当前输入组合的模型。新建空白节点选择候选模型时，必须把节点自带的可编辑 User 文本入口视为一个可用文本输入，让用户能够先选择平台和模型；真正运行时仍按实际内容校验，User 内容为空不得提交。节点自身用户指令固定排在 User 内容最前；外部文本按输入连线创建顺序追加；图片、视频和音频作为同一次多模态 User 消息发送。每次运行创建新的文本素材结果，不覆盖源素材。能力档案已登记但平台适配器未完成时必须明确阻止运行，不能把请求字段存在误写成全链路可用。
- 音频生成与音乐生成必须是两个独立节点：音频生成只承载语音、口播、音色和音效，音乐生成只承载歌曲、纯音乐、伴奏和配乐；两者均输出音频素材，但平台、模型、参数档案、能力筛选、预检和接口路由彼此独立。音乐节点使用 `music_generation` 和 `/api/canvas-music`，不得把音乐模型回退到音频节点或把语音模型显示到音乐节点。
- 新版用户界面统一将上传素材和生成结果称为“素材节点”；“统一媒体节点”只作为技术说明，不作为用户界面名称。素材节点根据媒体类型提供文本、图片、视频或音频对应的预览和操作，不能把图片编辑工具直接套用到视频或音频。
- 视频和音频素材必须支持时间范围裁剪。裁剪使用统一时间轴选择起点和终点，输出新的派生素材节点和新文件，原始素材、原始名称和原始引用保持不变；裁剪结果不能覆盖源文件，也不能只保存为画布临时状态。
- 视频和音频裁剪结果沿用源文件基础名称并追加时间范围后缀，例如 `原文件名_3s-5s.mp4` 或 `原文件名_3s-5s.mp3`；若同名结果已存在，使用稳定去重规则避免覆盖。
- 所有会改变媒体内容的裁剪、剪辑、抽取和编辑操作都按“新生成结果”处理：图片裁剪/扩图/遮罩/画笔/宫格/放大、视频时间范围裁剪、音频时间范围裁剪、视频抽取音频，均生成新的结果文件、结果索引和素材节点，不能覆盖源文件。
- 视频素材必须支持抽取音频。抽取出的音频进入 `assets/output/audio/` 和素材库“生成结果 / 音频”，并保存 `source_asset_id`、来源视频、抽取方式、时间范围和处理参数。
- 文本素材默认以安全 Markdown 预览；双击进入编辑草稿，保存后生成新的文本结果并保留原文。使用文本模型优化时也必须生成新结果，并保存原文本 ID、平台、模型、用户指令和生成参数，不能直接覆盖历史结果。
- 文本素材的非编辑预览必须渲染完整原文，不得按固定行数或固定字符数截断后在节点底部留下空白。节点当前高度只决定可见范围：用户拖大节点时继续显示后续正文，超出范围的内容由节点内部滚动承接；编辑状态和预览状态必须读取同一份完整文本。
- 智能画布中的所有文本编辑框必须保持编辑连续性。用户在编辑时切换到其他浏览器标签页、子页面或其他软件，再返回同一画布后，必须恢复原编辑框的焦点、光标/选区和编辑框内部滚动位置；不得跳回文本开头。任何会替换编辑器 DOM 的画布重渲染、配置刷新或协作同步，都必须先捕获并在替换后恢复这组状态，不能只为某一种节点或某一个提示词框单独处理。
- 画布不再管理或执行内置 Skill；Skill 在外部 Codex／Agent 中运行，产出通过 Agent 接口填入画布。保留 LLM 执行节点、普通提示词库和系统提示词。历史结果中的 Skill 快照只供追溯，不得重新加载到新任务。
- 输入变化导致当前模型失效时，不得静默切换模型；必须指出不兼容的输入类型、数量或参数，并要求用户重新选择模型。API 设置中已配置的平台入口应继续可见，并标明已配置模型数量和当前输入下的可用数量；不兼容平台可以禁用，但不得整个平台消失。具体模型候选仍只显示能力档案确认支持当前输入组合的模型，不能为了“看得见”而放宽运行校验。
- 设计或接入模型参数前，必须核对当前平台的官方 Schema、官方文档和项目真实请求适配器。模型名称、模型类别、模型卡或同类模型经验不能单独证明某个参数可用；没有接口证据的能力只能标记为待确认，不得自动生成可运行控件。
- 模型输入约束必须和参数一样写入能力档案，并由所有执行节点共用的后端运行前预检执行。数量、文本字符数、文件字节数、图片/视频宽高与总像素、单段媒体时长和同角色媒体总时长只能依据官方 Schema、官方文档或当前适配器证据登记；不得按模型名称猜测。点击运行后必须先读取本地素材真实元数据并完成校验，再创建结果占位或请求第三方接口；档案声明了约束但元数据无法读取时必须阻止运行并指出具体素材和缺失信息。此类运行前错误同样写入日志。
- 模型能力登记必须区分：模型必填输入、模型可选参数、模型高级参数、画布素材处理、用户效率工具和平台内部协议字段。只有前三类进入模型参数界面；裁剪、缩放、遮罩绘制、视频裁剪等属于素材处理，提示词优化、模板、循环等属于效率工具。
- 面向用户展示模型可用性时必须使用能直接辅助选择的文案，例如“建议选择”“可能不可用”，不得把“目录已列出”“Schema 已登记”等内部数据来源状态直接当作决策标签。技术含义和证据边界放在说明或悬停提示中，并明确“公开目录列出”不等于账号一定可运行，“公开目录未列出”也不等于一定不可运行。模型清单达到需要重复勾选的规模时，必须同时提供“全选建议”“全选全部”和“清空”等批量操作；风险确认在最终应用时合并提示，不得逐项打断用户。
- 能力全集只用于登记和筛选，不能直接等同于节点固定表单。节点保留稳定容器，当前模型经证实支持的输入和参数才动态显示；不支持的旧值可以保留草稿，但必须标记为不适用且不得发送。
- 文本、图片、视频、音频、音乐五类模型执行节点必须共用稳定的参数面板骨架。控件类型由参数语义决定，同一个参数键跨平台、跨模型保持同一种交互，不得再根据选项数量临时在按钮、下拉框和输入框之间切换。存在官方选项时优先让用户选择；只有 ID、自由文本等无法预设的值才允许手工输入。
- 文本、图片、视频、音频、音乐五类模型执行节点必须始终保留“平台、模型、运行模式”三个一级入口，即使当前模型只有一个运行模式也不得隐藏；桌面端三项优先在同一行三列紧凑排列。模型参数入口尚未选择时显示参数名称，例如“分辨率”；用户明确选择后改为显示当前值，例如“2K”。具体选项、滑块、步进器和自由输入只有在用户点击对应入口后才能从入口上方向上展开，悬停和聚焦不得自动打开；同一时间只能展开一个参数，点击下一个入口必须关闭上一个，点击画布或参数面板空白区域必须全部关闭。打开参数时必须同时提升参数面板、当前控件和弹层的层叠层级，确保弹层始终盖住相邻参数、节点、连线和画布工具；验收必须检查弹层中心的实际命中元素，不能只看 `z-index` 数字。分辨率、画面比例、时长、数量、布尔等相同语义参数跨节点共用同一入口、预览和弹层格式。执行面板头部必须使用统一高度和中心线对齐标题、模型信息、帮助按钮与运行按钮，分隔线由整个头部统一绘制；禁止靠不同 margin 或 padding 拼出近似对齐，也禁止把 Schema 参数及其选项重新全部平铺成大卡片或长表单。
- 模型固定选项必须显示具体值，不能提供含义不明的“默认”。按已验证 Schema 的初始值初始化；没有声明初始值的固定枚举使用合法选项并显式展示，布尔显示“是/否”。可选自由文本或缺少可确定初始值的字段可标为“未设置（不发送）”，不得伪造任务 ID、素材引用等上下文。界面显示值必须与真实提交值一致。
- 执行节点的模型说明必须随当前模型、运行模式和参数选择更新，并同时说明当前用途、输入、输出和已选参数效果；文本、图片、视频、音频、音乐五类模型执行节点保持同一说明结构，不能只为音频或个别模型实现。
- 执行节点顶部输入引用条与编辑框内的 `@引用` 必须复用同一套文本、图片、视频和音频悬停预览逻辑。顶部媒体缩略块使用独立圆角裁切层，标签和删除按钮可位于裁切层外；横向滚动条不得显示成素材缩略块底边。视频引用预览允许用户听到原始声音，不得默认静音。文本生成参数区不得再用“输入数量 / 暂无外部输入”等汇总条重复表达顶部引用状态。
- 任何向用户提供“打开日志”入口的错误提示都必须先写入画布日志，再显示提示；日志不能只记录任务成功或失败终态。模型与输入不匹配、缺少必填内容、参数校验失败、适配器不可用等运行前错误也必须记录节点、平台、模型、提示词和错误原因。同一节点的同一错误在短时间内应去重，避免业务日志与提示日志重复出现。
- 文本、图片、视频、音频、音乐和 AI 应用执行节点只负责提交任务，不承载单次任务的运行、计时、失败或取消状态；每次运行必须创建一个独立结果占位素材节点，并允许同一执行节点继续提交并行任务。占位节点负责显示运行中与计时、提供“停止运行”、保存本次 `runRef/taskId`，成功后原位转成对应素材；失败后保留该占位并以红色感叹号、失败标题和具体原因呈现。主动停止后停止本地等待和结果回写、尽力取消可控的后端任务，并删除该空占位及连接；第三方平台没有取消接口时必须明确说明上游任务可能继续，不能伪装成已经撤单。失败与主动停止是两种状态，停止不得显示为失败。
- 用户从素材节点“生成信息”按固定记录重新生成时，运行状态由当前素材节点承载：保留旧素材作为背景并明显压暗，显示“重新运行中”、实时计时和取消按钮；弹窗底部按钮必须同步切换为取消操作。只有成功后才新增并切换结果版本；失败保留旧版本，主动取消立即恢复旧素材、停止本地等待和结果回写，不得新增版本或显示失败。输入素材引用保持只读，但文本、图片、视频和音频引用卡片及输入文本中的 `@引用` 都必须复用执行节点的悬停预览。
- 生成结果素材节点可以继续接收新的输入连线，也可以在“生成信息”的输入内容区域添加素材；新增、删除和排序必须复用执行节点的引用数据结构。结果节点重新生成时固定使用该版本记录的平台、模型和运行模式，只更新同一节点的结果版本集合，不得创建重复结果节点。画布状态合并时必须按稳定版本 ID 或媒体 URL 合并 `resultVersions`，并优先保留版本更多一侧的当前版本，不能让旧远端快照覆盖新生成的第 3 个及后续版本。
- 云端排队位置只有在平台返回可持续轮询的真实位置和总数时才允许显示。位置与总数必须是正整数且位置不得大于总数；`0/0`、负数、空值、非数字、过期值或推测值一律不得展示。轮询缺少可信队列数据时必须清除旧数字，并使用“云端处理中”等不声称具体排队位置的中性状态；不得为了界面看起来完整而伪造队列进度。
- 种子、负面提示词、采样器等低频参数只有在当前模型接口明确支持时才进入“高级参数”，不得因部分模型存在就成为同类节点默认控件。遮罩由画布生成素材；只有模型接口明确接受 `mask` 时，才作为特殊图片输入角色提交。
- RunningHub AI 应用的配置责任固定在官方 Schema：用户只提供 AI 应用 ID，系统自动同步标题、封面、说明和字段定义。禁止要求用户手工判断字段类型、输入名称、顺序、选项或映射；无效 ID 验证失败时不得写入配置。
- 新建 `RunningHub ComfyUI` 节点只能选择已同步的 AI 应用并调用 AI 应用接口。禁止在该节点暴露 RunningHub 工作流、工作流 ID、工作流 JSON 或 24G/48G 机器规格；历史画布和 MiniMax 工作流只能走独立兼容入口。
- RunningHub 官方字段必须同时保留用户可见标签和真实提交值。文本、图片、视频、音频自由输入按字段逐项映射并支持同类多输入；下拉、是否、数字和布尔字段按官方类型渲染，不得把选项显示文案代替 `index/value` 提交。
- RunningHub 多字段映射必须把稳定目标键 `nodeId::fieldName` 写入连接的 `targetFieldKey`，不能只存在临时 UI 状态或依赖数组顺序。来源素材可显示官方可读字段名；单槽自动绑定，多槽在落线时要求选择，满槽明确替换，保存重开和运行提交必须读取同一映射。参数面板不得再提供第二套来源模式、素材选择或槽位交换控件。
- RunningHub 落线触发字段选择器时，打开选择器的同一次指针/鼠标/点击事件不得被全局“点击外部关闭”逻辑再次消费。选择器必须持续显示到用户选择字段、明确点到外部或按 `Esc`；取消时不写入半条连接。验收必须覆盖多同类槽位选择和同类槽位已满后的替换两种路径。
- RunningHub `fieldData` 可能是 JSON 字符串，API 设置和智能画布必须共用结构化解析语义。旧快照缺少选项或上传类型时从已脱敏官方快照补齐；显示标签与提交值分离，`STRING/TEXT`、`SWITCH/SELECT`、`FLOAT/NUMBER` 等等价类型及显式顺序 `0` 不得误报 Schema 变化。
- `RunningHub ComfyUI` 节点保留原有封面/图标、标题、平台信息、应用 ID、底部输入统计、参数入口状态和卡片结构，仅用当前 AI 应用名称替换中部字段明细区，不在中部重复显示输入槽位。应用参数使用独立垂直面板，文本、图片、视频、音频输入与普通参数合并在同一清单，并严格按官方 `nodeInfoList` 顺序展示，禁止按字段类型重排。媒体字段未连接时显示官方类型对应的原生输入，连接后自动显示绑定素材，不出现“直接输入 / 外部节点”模式开关。面板只能右侧优先、下方兜底，禁止放到节点左侧；空间不足时参数清单内部滚动，面板不得覆盖节点或透出底层内容。
- 用户要求替换节点某个局部区域时，只修改被明确指向的区域。未被点名的封面/图标、标题、平台信息、应用 ID、底部输入状态和卡片结构必须保留；不得把“中间详情改为应用名称”扩大成“整个节点只保留应用名称”。
- 固定高度并内部滚动的参数清单中，每个参数行必须禁止 Flex 收缩，并由自身控件内容撑高；长文本、媒体上传和已连接素材不得溢出到下一行。验收除检查相邻行边界不相交外，还必须检查每个控件的上下边界完全位于所属参数行内。短文本可使用紧凑初始高度，长文本保持多行编辑空间，但不得改变官方字段类型和提交语义。
- RunningHub AI 应用计费单位的用户可见中文名称统一为“RH币”，英文统一为“RH Coin”。不得写成“RunningHub币”或“Running Hub币”；平台、参数和节点中的品牌名 `RunningHub` 保持原样，内部协议字段不因文案统一而改名。
- 保存 RunningHub 官方响应前必须在后端做白名单脱敏。示例 `curl`、API Key、令牌和未经批准的原始字段不得进入 `data/api_providers.json`、画布、工作流导出或日志。

禁止做法：

- 只根据口述需求画一套理想化新架构，却不核对当前代码是否已有同类能力。
- 为了节点拆分而缩水、重复实现或静默删除现有功能。
- 把其他产品的界面和逻辑原样照搬到本项目。
- 在未设计旧画布、旧节点和旧数据迁移方式前直接替换现有结构。
- 把“能够读取旧文件”误解成“旧节点必须长期可见、可编辑、可运行”。
- 根据模型类别或个人经验猜测参数，再把猜测写成节点固定能力。
- 把用户选中的不支持参数静默丢弃，让用户误以为该参数已经参与运行。

## 视频导演台长期规则

智能画布中的视频导演台使用一套共享骨架，并保留两个明确入口：“通用导演台”面向标准视频 API，“MiniMax H3 导演台”面向本地 ComfyUI 或 RunningHub 个性化工作流。两者的工程素材库、顶部预览、Clip、单轨时间线、垂直输入区、结果版本和分片导出必须共用同一数据契约；不得因为后端调用方式不同而复制第二套前端时间线。所有导演台只生成视频，不承担字幕、配乐、多层合成和最终剪辑。

- 画布连线到导演台只登记进工程素材库，因为连线本身没有目标 Clip 语义；用户把资产直接拖到具体 Clip 时，同时登记工程素材库并绑定该 Clip，不能强迫用户先拖素材库再拖 Clip。
- 当前 Clip 的文本、图片、视频和音频必须在 Clip 下方按类型分组并垂直罗列；提示词中的 `@文本1`、`@图片1`、`@视频1`、`@音频1` 与同一稳定素材索引和提交顺序一致。
- 普通 Clip 与连接 Clip 位于同一条轨道。连接 Clip 是覆盖在普通 Clip 上的半透明层，不是上方第二条轨道；连接 Clip 之间禁止互相覆盖。
- 连接 Clip 左侧覆盖普通 Clip 尾部小于 1 秒时使用上一 Clip 尾帧，右侧覆盖下一 Clip 开头小于 1 秒时使用下一 Clip 首帧；任一侧达到或超过 1 秒时，精确截取该侧实际覆盖的视频片段。视觉对齐后的零重叠边界仍必须保留已经确认的尾帧或首帧关系，保存刷新后不得丢失。
- 每个 Clip 都是独立的视频生成执行单元，独立保存平台、模型、运行模式、参数草稿、运行状态和结果版本。通用 Clip 必须复用普通视频节点的能力档案、动态参数控件、预检和执行链；参数提交使用 `capabilityParameters[model]`，未被当前 Schema 支持的旧草稿不得发送，也不得让其他模型从候选列表中消失。
- 连接 Clip 的首帧、尾帧和视频区间只在运行前物化到 `cache/director-references/`，属于可重建缓存，不得伪装成用户资产或生成结果。
- 导演台导出的是独立视频片段压缩包，不拼接成片。普通和连接 Clip 统一按时间中点、开始时间、创建时间和稳定 ID 排序，文件统一命名为 `工程名-clip-NNN.ext`，类型差异只写入清单。
- 角色—音色绑定只保留在设计文档的后续记录中。在用户再次明确确认前，不新增字段、界面、自动匹配或请求逻辑。

## UI 设计规范与技能调用

所有新的 UI 设计、页面重构、组件样式和交互视觉修改，必须先阅读并遵守 `docs/UI设计与美学规范.md`。该规范是本项目的统一视觉基线，不能只按当前页面的局部旧样式继续堆叠。

执行 UI 工作时必须同时调用并阅读以下两套设计技能：

- `/Users/a1/.agents/skills/frontend-design/skills/frontend-design/SKILL.md`
- `/Users/a1/.agents/skills/design-taste-frontend/SKILL.md`

前者用于明确产品目的、用户、设计方向和高质量实现方式，后者用于已有页面审计、设计刻度、反模板化检查和发布前验收。两套技能的建议必须结合当前项目的中文、多平台、画布节点和数据约束使用，不能脱离现有产品直接套用营销页面范式。

每次 UI 修改至少要完成一次真实浏览器验证，并检查浅色主题、深色主题、窄窗口、焦点状态、弹层层级和可见文案。只有新增设计令牌、组件规则或长期复用的交互模式时，才更新设计规范，不能把一次性的临时样式写成全局规则。

## 精简与 Agent 接入边界

- 独立在线生图、GPT 对话页面、Photoshop 连接器及 DX 装饰已经退役，默认进入无限画布；作者名称统一为“老胡用AI画梦”。
- 精简前必须追踪当前画布、素材库和保留工具的调用关系；公共模型适配器、用户素材、画布、历史结果、许可证与归属说明不能作为克隆残留删除。
- Agent 操作复用当前节点契约、参数能力档案、预检、结果占位与保存流程。不能直接执行 Agent 提供的 JavaScript 或 Shell；任务只允许结构化命令，重复命令不得重复产生付费请求。

## Agent 默认配置与连接体验

- Agent 接入说明必须使用当前实际服务地址和画布 ID，兼容启动器打开的本机局域网地址；不得要求用户把地址改成固定的 127.0.0.1。控制接口仍区分服务器自身网卡地址和其他局域网电脑。
- 复制说明、读取节点设置、保存默认等弹窗内操作，在弹窗内给出可读状态，不得把反馈显示到背后的画布。
- 各类 Agent 默认模型和参数保存在当前画布，Agent 创建节点时以默认配置为基础，只覆盖用户明确指定项；换平台或模型时重新检查能力，不能携带不兼容参数。未配置类型不得静默猜测付费模型。
- 默认设置和执行节点复用能力档案与语义控件，固定选项显示并提交明确值，允许留空的自由输入标明“不发送”；保留布尔 false 和数字 0，不另建前端参数清单。


## 素材库精简与操作一致性

- 素材库不提供内置智能分类、偏好设置和平台注册入口；上传、导入不得自动调用分类模型。分类判断交给外部 Agent，复用现有分组、收藏和移动接口，不影响系统提示词、普通提示词和 LLM 节点。
- 分组创建只出现在所属库或文件夹的顶层操作区；子分组仅提供重命名和删除，禁止从子分组入口创建同级分组，造成会创建下一级的错觉。
- 输入素材、临时素材与生成结果详情共享操作顺序：预览、文本编辑（仅文本）、下载、改名、收藏（适用时）、删除。相同操作使用相同图标、尺寸和中英文提示，删除放最后；不增加与下载重复的打开链接、复制链接按钮。
- 删除上述功能只清理专用界面、请求及无调用代码；历史素材、分组、已有平台素材引用和稳定文件 URL 保留，不能连带删除用户数据。


## 节点参数选择与手动继承

- 参数入口和选项必须完整显示文字；长模型名、分辨率和选项允许换行，不截断字符串或裁切半个字。五类生成节点共用字号、间距、弹层、拖拽手柄和焦点样式。
- 手动新建执行节点沿用同类节点最近编辑的模型、运行模式和模型参数，按节点类型独立记忆；不得复制正文、输入素材、连线或运行状态。读取或浏览旧节点不得覆盖最近编辑记录。
- Agent 创建继续只使用当前画布的 Agent 默认配置及本次明确覆盖项；不得读取手动新建的记忆。模型不再启用或缺少所需输入时保留选择并提示校验，不静默换成另一个模型。


## 素材呈现与系统剪贴板

- 侧栏收缩时显示“老胡”，展开后显示完整作者名；图标、文字使用固定列，作者名和社交图标共享居中轴，不能靠裁切全称适配窄栏。
- 生成结果按文本、图片、视频、音频、音乐排序。音乐依据生成节点或明确分类标记识别，底层仍是音频文件；收藏、移动与历史版本迁移保留该分类，不按文件名猜测，也不能把音乐任务的输入语音归成音乐。
- 视频缩略图展示真实首帧；服务端无法解码时使用浏览器读取原视频帧。音频使用波形、音乐使用音符，不能用黑卡或统一音符掩盖媒体类型。
- 展开预览在左上角显示 ESC 退出说明，右上角显示点击黑色背景退出说明；提示不能遮挡媒体操作。中英文与实际退出行为同步。
- 素材库批量工具共用“全选／清空”单一切换控件、下载所选、删除所选；全选状态由当前可见素材计算，部分选择显示未全选状态。不要重新堆叠复制、复制到画布等重复按钮。
- 素材库 Command/Ctrl+C 复制选中的原素材，勾选集合优先于单项焦点；画布 Command/Ctrl+C 仍复制节点，Command/Ctrl+Shift+C 复制原素材。输入文本时保留系统文本复制，不能截获编辑器的选区。
- 单图片复制包含图片数据，多个或混合媒体复制原文件集合；不能退化成网址却提示素材已复制。非安全 HTTP 地址使用本机剪贴板桥接时，只允许本机客户端、同源请求及项目受管媒体，不能接受任意文件路径或远程下载。
- 复制连接说明不得选中可见正文；成功后在按钮旁或按钮本身以绿色“已复制，去粘贴”提示，失败必须明确显示原因。


## 参数编辑与画布资源包

- 所有模型数值滑块旁必须提供可直接输入的数值框；滑块和输入框双向同步，共用模型的类型、上下限和步长。输入中的空串或不完整数字不能静默写成 0；失焦显示实际有效值。
- 比例参数优先使用接口明确提供的枚举。已证实允许自由比例且未限定选项时，提供常用比例和自定义输入，不留一个空白文本框；不得为模型添加接口未声明支持的比例参数。
- Agent 默认配置在弹窗内直接选择平台、模型、应用或工作流，不再依赖“从选中节点读取”和“清除此类默认”按钮。保存后必须验证持久化，以及 Agent 创建节点实际使用的值。
- 列表和画布内的“导出并包含资源”必须复用同一 ZIP 格式及后端。包内保存 workflow.json、resources/ 和明确的资源映射，文件保留中文原名并避免重名覆盖；稳定资源 URL、历史版本、引用 HTML、节点内部引用与连线必须一并映射。
- ZIP 在新画布内直接导入，不要求用户解压、重命名或手工指定映射。导入使用当前素材与结果存储和稳定接口，按内容去重；验证包完整性后才写入正式目录。密钥不随包导出；缺失资源不得静默跳过后提示完整导出。JSON 单独导出必须说明不包含资源。
- 导入要支持历史 canvas.json/resources-manifest.json 包，不能继续生成另一套不兼容的新包。移除“剪切到其他项目”及其专用粘贴入口，保留正常项目组织能力。

- 用户用截图指出局部控件问题时，按参数语义检查同类入口：执行节点、Agent 默认配置、生成信息、素材处理。相同语义必须复用控件与事件逻辑，不能分别复制近似样式。数值统一提供拖动与直接输入；枚举、比例、布尔统一选择入口、选中反馈、展开方向、字号与间距，只有内容和接口约束不同。素材处理的时间、笔刷、间距和音量同样适用，不能只修模型参数。


## 整理选中的宫格对齐

“整理选中”保留用户已摆放的空间关系，只将选中节点就近对齐到世界坐标中的隐形宫格中心；不得按连线重新分层，也不得因只选一个节点而连带移动整条链。基准来自默认素材节点尺寸，大节点按尺寸占据多格并对齐联合中心，略微越界不额外占格。遇到占位冲突选择附近可用位置，未选中节点不移动；分组作为整体平移并保持成员相对位置。整理不缩放节点、不改连接、素材或版本，必须支持撤销，重复整理保持稳定。Agent 的 arrange 复用同一算法。


## 参数入口对齐与引用交换

平台、模型、运行模式一级入口保持单行等高，模型列优先获得更多宽度；长名称省略显示并在悬停及展开列表提供完整内容，不得因模型名换行破坏对齐。窄屏使用统一纵排。
输入引用的同类交换以引用内容有效性为条件，不以有无连线为条件；直接添加和连线引用可以互换。普通拖拽只交换当前节点引用顺序及提示词位置；Shift 拖拽沿用全局内容交换，兼容无源节点素材，更新已有引用及序号，不复制媒体文件、不自动创建连线。不同媒体类型不互换，保存重开和实际输入必须保持一致。


参数入口和选项的文案只表达名称或值：优先“时长、画幅、分辨率、默认、是、否”和“5秒、16:9、2K”等短标签；解释留在帮助提示，禁止将说明长句放入选项。参数界面格式统一、内容遵循模型能力档案；时长等有限范围优先按合法步长生成点选格，官方枚举原样保留。默认项单独整行，不参与数字/画幅选项排序，不得挤成竖排；有大量选项时用内部滚动承接。

## 运行状态动效连续性

- 运行中的视觉效果以任务 ID 管理生命周期；选中、拖动、缩放、调整节点尺寸、主题切换和画布重绘不得重建或重启动画。动画和计时不反向修改任务状态。
- 采用中性底色、局部低对比柔光与缓慢变化的光点，随机位置按任务固定轨迹连续播放，不在每次渲染时重新随机。空结果不得透出画布网格形成脏灰背景；已有结果保留可辨认的压暗预览。
- 成功、失败、取消或删除节点后必须释放动画；尊重系统减少动态效果设置。验收同时检查真实画布操作前后的动画实例和播放时间，以及空结果、有旧结果、明暗主题和取消操作。

## 媒体编辑工具栏与视觉验收

- 音频与视频编辑共用“名称与唯一关闭入口、内容/时间轴、集中工具栏”的结构。播放、试听、音量、裁剪、提取和导出必须按职责分组并对齐到同一工具栏，禁止新增功能后在旧界面底部继续追加孤立按钮行。
- 工具栏统一按钮高度、间距和边界；窄窗口按完整功能组换行，不得留下横跨整行却只有一个小按钮的空面板。相同的关闭操作只保留顶部入口。
- 截图验收先检查整体布局、空白和操作分组，再检查功能是否可点击；“功能测试通过”不能替代排版验收。
- 新增媒体操作前，检查节点工具栏和预览弹窗中的既有入口。同一弹窗不得同时保留“提取尾帧”和“导出尾帧”等实质重复按钮；首帧、当前帧、尾帧统一创建结果素材，下载复用结果节点入口。
- 音视频裁剪使用共享时间轴，包含起止拖柄、独立播放头、点击定位和选区试听。波形必须来自真实音频；无法解码时明确提示并保留时间轴，不得用装饰柱形伪造波形。
- 音视频裁剪、提取帧和提取音频产生独立结果，放在源节点右侧并用虚线表达派生关系，不能作为实线输入引用或覆盖原文件。媒体工具必须检查能否实际启动，不能仅凭 PATH 中存在文件就认定可用。


## 升级事务与旧安装边界

- 升级统一复用 `canvas_update.py`：正式 R2 和固定提交 GitHub 经过同一校验、依赖准备和启动检查，不保留在线逐文件覆盖的第二条路径。
- 依赖变化时创建独立环境，禁止在运行中的旧环境执行 pip 升级。只有新版真实就绪才能显示升级成功；失败恢复或未受管理的服务必须明确告知。
- 旧安装更新器无法识别新目录时，提供独立过渡升级入口并在发布说明指出适用版本，不能只测试新版升级器后宣称所有旧版已兼容。
- 升级回归必须包含暂存损坏、依赖安装失败、替换中断、启动失败、环境回退与用户数据/素材保留。Mac/Windows CI 不替代 Windows 桌面和具体旧安装实机验证。
